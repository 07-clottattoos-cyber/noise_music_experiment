# AGENTS.md

本项目用于完成“噪声音乐·随机旋律的发展”实验。

开发要求：
1. 使用 Python 3.10+。
2. 代码应清晰、可读、可复现。
3. 所有随机生成必须接受 seed 参数。
4. 主入口为 `python -m src.main`。
5. 输出文件统一放在 output/ 目录下。
6. 不要把生成文件写到项目根目录。
7. 优先使用 music21 生成 MusicXML 和 MIDI。
8. 如果 MuseScore 可用，则调用 MuseScore 命令行导出 PDF 和音频；如果不可用，不要报错终止，只输出提示。
9. 统计结果必须可用于实验报告。
10. 每次修改代码后，请运行：
   - `python -m src.main`
   - `pytest`
11. 如果测试失败，请修复后再总结。
12. 请避免使用复杂外部依赖，除非已经写入 requirements.txt。
13. 注释和报告内容使用中文。
